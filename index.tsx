import React from 'react';
import ReactDOM from 'react-dom';
import Chat from './vercel_nuxt/app/page';

ReactDOM.render(
  <React.StrictMode>
    <Chat />
  </React.StrictMode>,
  document.getElementById('root')
);